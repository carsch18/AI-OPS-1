# AIOps Email Notification Configuration
# Copy this to .env and update with your SMTP credentials

# Enable email notifications (set to 'true' to enable)
EMAIL_ENABLED=true

# SMTP Server Configuration
# For Gmail: use smtp.gmail.com with App Password
# For Outlook: use smtp.office365.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# SMTP Credentials
# For Gmail: create an App Password at https://myaccount.google.com/apppasswords
SMTP_USER=adityatiwari@sigmainfo.net
SMTP_PASSWORD=koad udqg zcip poux

# Email addresses
SMTP_FROM=aditya.tiwari@sigmainfo.net
SMTP_TO=hrishikesh.kr@sigmainfo.net
