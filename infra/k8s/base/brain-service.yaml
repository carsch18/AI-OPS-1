apiVersion: v1
kind: Service
metadata:
  name: brain-api
  namespace: aiops
  labels:
    app: brain-api
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: brain-api
