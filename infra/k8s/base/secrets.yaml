# NOTE: In production, use External Secrets Operator or AWS Secrets Manager
# This is a placeholder showing the structure
apiVersion: v1
kind: Secret
metadata:
  name: aiops-secrets
  namespace: aiops
type: Opaque
stringData:
  # Database (reference from AWS Secrets Manager in prod)
  DATABASE_URL: "postgresql://aiops_admin:CHANGEME@rds-endpoint:5432/aiops_brain"
  
  # Redis
  REDIS_URL: "redis://redis-endpoint:6379"
  
  # JWT Secret
  JWT_SECRET: "CHANGE-THIS-IN-PRODUCTION-USE-SECRETS-MANAGER"
  
  # Cerebras API Key (inject from Secrets Manager)
  CEREBRAS_API_KEY: "INJECT-FROM-SECRETS-MANAGER"
