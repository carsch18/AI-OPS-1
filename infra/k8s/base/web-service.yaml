apiVersion: v1
kind: Service
metadata:
  name: web-ui
  namespace: aiops
  labels:
    app: web-ui
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: web-ui
